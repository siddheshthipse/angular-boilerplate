import { NiceDateFormatPipePipe } from './nice-date-format-pipe.pipe';

describe('NiceDateFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NiceDateFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
